apiVersion: v1
kind: Service
metadata:
  name: {{ include "helloworldwebapi.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "helloworldwebapi.name" . }}
    helm.sh/chart: {{ include "helloworldwebapi.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "helloworldwebapi.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
